<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <title>Eliminar</title>
    <%- include("partials/head") %>
  </head>
  <body>
    <%- include("partials/return_eliminar") %>
    <div class="container p-4">
      <div class="row">
        <div class="col-md-4 offset-md-4">
          <div class="card animated fadeInDown">
            <img src="<%= image.path %>" class="card-img-top" />
            <div class="card-body">
              <h5 class="card-title"><%= image.nombre %></h5>
              <p class="card-text"><%= image.apellidos %></p>
              <p class="card-text"><%= format(image.create_at) %></p>
              <a href="/image/<%= image._id %>/delete" class="btn btn-danger">Eliminar</a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
